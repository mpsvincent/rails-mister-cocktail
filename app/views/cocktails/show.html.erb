<h1>Cocktails#show</h1>
<p>Find me in app/views/cocktails/show.html.erb</p>


<p>
  <strong>Name:</strong>
  <%= @cocktail.name %>
</p>


<% @doses.each do |dose| %>
<p><%= "#{dose.description} of #{dose.ingredient.name}" %></p>
<td><%= link_to 'Delete', dose_path(dose), method: :delete %></td>

<% end %>

<%= link_to 'Back', cocktails_path %>
<%= link_to 'Add details', new_cocktail_dose_path(@cocktail) %>